name: Filter Branch Workflow
on:
    push:
        branches:
            - main
        paths:
            - '!README.md'


jobs:
    checkout-branch:
        runs-on: self-hosted
        steps:
            - uses: actions/checkout@v4

            - run: git branch

