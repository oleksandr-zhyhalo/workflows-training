name: Hello World Workflow
on:
    workflow_dispatch: 

jobs:
    say-hello:
        runs-on: self-hosted
        steps:
            - name: Print message
              run: echo "${{ secrets.MY_EXAMPLE_SECRET }}"